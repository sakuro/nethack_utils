#!/usr/local/bin/ruby
# -*- encoding: US-ASCII -*-

require 'nethack_utils'
require 'gettext'

include GetText
bindtextdomain('nethack_utils')

arg = ARGV.shift
today = arg ? Date.parse(arg) : Date.today

motd = today.motd
puts motd[:fri13th] if motd.key?(:fri13th)
