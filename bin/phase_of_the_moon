#!/usr/local/bin/ruby
# -*- encoding: US-ASCII -*-

require 'nethack_utils'
require 'gettext'

include GetText
bindtextdomain('nethack_utils')

today = Date.today

motd = today.motd
puts motd[:pom] if motd.key?(:pom)
