#!/usr/local/bin/ruby
# -*- encoding: US-ASCII -*-

require 'nethack_utils'
require 'gettext'

include GetText
bindtextdomain('nethack_utils')

arg = ARGV.shift
today = arg ? Date.parse(arg) : Date.today

motd = today.motd
puts motd[:pom] if motd.key?(:pom)
